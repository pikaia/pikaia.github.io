---
layout: post
title: "From Amah to Auntie: How Singapore Came to Rely on Foreign Domestic Workers"
date: 2026-07-18 14:00:00 +0800
last_modified_at: 2026-08-28 16:30:00 +0800
categories: [society, present-day]
image: https://upload.wikimedia.org/wikipedia/commons/0/04/Lucky_Plaza%2C_Orchard_Road%2C_Singapore.jpg
---

Before there were foreign domestic workers, there were amahs — Cantonese women, some of them majie who had taken vows never to marry, who ran Singaporean households through the mid-20th century. By the 1970s they were a vanishing breed: as female literacy rose from 27% in 1957 to over 60% by 1970, the young women who might once have gone into domestic service were going into factories and offices instead. Singapore's households still needed the help. It just had to come from somewhere else.

[← Back to all posts](/)

<!-- WATCH-WIDGET:BEGIN (generated by scripts/build_watch_widget.py - do not hand-edit between these markers, re-run the script instead) -->

<div style="display: flex; gap: 2em; margin: 0.5em 0 1.5em 0; align-items: flex-end;">
  <div id="listen-widget" role="button" tabindex="0" aria-label="Play audio narration of this post" style="display: inline-flex; flex-direction: column; align-items: center; cursor: pointer; gap: 0.2em; user-select: none;">
    <span id="listen-icon" aria-hidden="true" style="display: inline-flex; align-items: center; justify-content: center; width: 2.1em; height: 2.1em; border-radius: 50%; border: 1px solid #888; font-size: 1.1em;">&#127911;</span>
    <span style="font-size: 0.7em; text-transform: uppercase; letter-spacing: 0.05em; opacity: 0.75;">Listen</span>
  </div>
  <div id="watch-widget" role="button" tabindex="0" aria-label="Watch an animated version of this post" style="display: inline-flex; flex-direction: column; align-items: center; cursor: pointer; gap: 0.2em; user-select: none;">
    <span aria-hidden="true" style="display: inline-flex; align-items: center; justify-content: center; width: 2.1em; height: 2.1em; border-radius: 50%; border: 1px solid #888; font-size: 1.1em;">&#127916;</span>
    <span style="font-size: 0.7em; text-transform: uppercase; letter-spacing: 0.05em; opacity: 0.75;">Watch</span>
  </div>
  <a href="https://youtu.be/pi7TazuEv6U" target="_blank" rel="noopener" aria-label="Watch this story as a video on YouTube" style="display: inline-flex; flex-direction: column; align-items: center; gap: 0.2em; text-decoration: none; color: inherit;">
    <span aria-hidden="true" style="display: inline-flex; align-items: center; justify-content: center; width: 2.4em; height: 2.4em; border-radius: 50%; border: 1px solid #888;">
      <svg width="1.4em" height="1.4em" viewBox="0 0 24 24" aria-hidden="true"><rect x="1" y="4" width="22" height="16" rx="5" fill="#FF0000"/><path d="M10 8.5l6 3.5-6 3.5z" fill="#fff"/></svg>
    </span>
    <span style="font-size: 0.7em; text-transform: uppercase; letter-spacing: 0.05em; opacity: 0.75;">YouTube</span>
  </a>
  <a href="https://youtube.com/shorts/B6HPpTbmKV4" target="_blank" rel="noopener" aria-label="Watch a short version of this story on YouTube Shorts" style="display: inline-flex; flex-direction: column; align-items: center; gap: 0.2em; text-decoration: none; color: inherit;">
    <span aria-hidden="true" style="display: inline-flex; align-items: center; justify-content: center; width: 2.4em; height: 2.4em; border-radius: 50%; border: 1px solid #888;">
      <svg width="1.4em" height="1.4em" viewBox="0 0 24 24" aria-hidden="true">
        <defs>
          <linearGradient id="shortsGradFromAmahTo96fc" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#FF0000"/>
            <stop offset="100%" stop-color="#FF4E8B"/>
          </linearGradient>
        </defs>
        <rect x="1" y="1" width="22" height="22" rx="7" fill="url(#shortsGradFromAmahTo96fc)"/>
        <path d="M13 6 L7.5 13.5 H11.5 L10.5 18 L16.5 10 H12.5 Z" fill="#fff"/>
      </svg>
    </span>
    <span style="font-size: 0.7em; text-transform: uppercase; letter-spacing: 0.05em; opacity: 0.75;">Shorts</span>
  </a>
</div>

<audio id="listen-audio" preload="none" style="display: none;">
  <source src="/audio/from-amah-to-auntie-rise-of-domestic-workers.mp3" type="audio/mpeg">
</audio>

<div id="watch-viewer" style="position: fixed; inset: 0; background: #000; z-index: 9999; display: none;">
  <div id="watch-stage" style="position: absolute; inset: 0; overflow: hidden;"></div>
  <div style="position: absolute; left: 0; right: 0; bottom: 12%; text-align: center; padding: 0 5%; z-index: 2;">
    <span id="watch-caption" style="display: inline-block; background: rgba(0,0,0,0.55); color: #fff; font-size: 1.3em; font-weight: 600; padding: 0.5em 0.8em; border-radius: 4px; max-width: 700px;"></span>
  </div>
  <button id="watch-close" aria-label="Close" style="position: absolute; top: 1em; right: 1.2em; color: #fff; font-size: 1.6em; cursor: pointer; background: none; border: none; z-index: 2;">&times;</button>
  <div style="position: absolute; bottom: 0; left: 0; right: 0; padding: 1em 1.5em; display: flex; align-items: center; gap: 1em; background: linear-gradient(transparent, rgba(0,0,0,0.6)); z-index: 2;">
    <button id="watch-play" aria-label="Play/Pause" style="background: none; border: none; color: #fff; font-size: 1.4em; cursor: pointer; line-height: 1; padding: 0.2em;">&#10074;&#10074;</button>
    <div id="watch-progress" style="flex: 1; height: 4px; background: rgba(255,255,255,0.25); border-radius: 2px; overflow: hidden; cursor: pointer;">
      <div id="watch-progress-fill" style="height: 100%; width: 0%; background: #fff;"></div>
    </div>
    <span id="watch-time" style="color: #fff; font-size: 0.85em; font-variant-numeric: tabular-nums; opacity: 0.8;">0:00 / 0:00</span>
  </div>
</div>

<script>
(function () {
  var widget = document.getElementById('listen-widget');
  var icon = document.getElementById('listen-icon');
  var audio = document.getElementById('listen-audio');

  function setIcon(playing) {
    icon.innerHTML = playing ? '&#10074;&#10074;' : '&#127911;';
  }
  function toggle() {
    if (audio.paused) { audio.play(); } else { audio.pause(); }
  }

  widget.addEventListener('click', toggle);
  widget.addEventListener('keydown', function (e) {
    if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); toggle(); }
  });
  audio.addEventListener('play', function () { setIcon(true); });
  audio.addEventListener('pause', function () { setIcon(false); });
  audio.addEventListener('ended', function () { setIcon(false); });
})();
</script>

<script>
(function () {
  var HERO = "https://upload.wikimedia.org/wikipedia/commons/0/04/Lucky_Plaza%2C_Orchard_Road%2C_Singapore.jpg";
  var SERVANTS = "https://upload.wikimedia.org/wikipedia/commons/thumb/3/3d/KITLV_-_29190_-_Ten_domestic_servants_of_various_ethnic_origins%2C_each_with_an_object_relating_tot_their_task%2C_Singapore_-_circa_1900.tif/lossy-page1-1280px-KITLV_-_29190_-_Ten_domestic_servants_of_various_ethnic_origins%2C_each_with_an_object_relating_tot_their_task%2C_Singapore_-_circa_1900.tif.jpg";
  var DHOBY = "https://upload.wikimedia.org/wikipedia/commons/thumb/e/ec/KITLV_-_103979_-_Washing_and_bleaching_field%2C_Singapore_-_circa_1880.tif/lossy-page1-1280px-KITLV_-_103979_-_Washing_and_bleaching_field%2C_Singapore_-_circa_1880.tif.jpg";
  var TAILOR = "https://upload.wikimedia.org/wikipedia/commons/thumb/9/93/KITLV_-_29191_-_Klingalese_tailor_Singapore_-_circa_1900.tif/lossy-page1-1280px-KITLV_-_29191_-_Klingalese_tailor_Singapore_-_circa_1900.tif.jpg";
  var KNIFE = "https://upload.wikimedia.org/wikipedia/commons/thumb/e/e4/KITLV_-_50183_-_Lambert_%26_Co.%2C_G.R._-_Singapore_-_Malaysian_knife_sharpeners_in_Singapore_-_circa_1900.tif/lossy-page1-1280px-KITLV_-_50183_-_Lambert_%26_Co.%2C_G.R._-_Singapore_-_Malaysian_knife_sharpeners_in_Singapore_-_circa_1900.tif.jpg";

  var slides = [
    { src: SERVANTS, type: "letterbox", zoom: [1, 1.07, 1.14], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-in-out" },
    { src: DHOBY, type: "letterbox", zoom: [1.13, 1.06, 1], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-out" },
    { src: TAILOR, type: "letterbox", zoom: [1, 1.06, 1.12], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-in" },
    { src: HERO, type: "letterbox", zoom: [1, 1.08, 1.16], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-in-out" },
    { src: HERO, type: "letterbox", zoom: [1.14, 1.07, 1], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-out" },
    {
      type: "chart",
      xRange: [2009, 2025],
      title: "Foreign domestic workers in Singapore, 2009-2025",
      data: [[2009, 196000], [2010, 201400], [2011, 206300], [2012, 209600], [2013, 214500], [2017, 246800], [2018, 253800], [2019, 261800], [2020, 247400], [2021, 246300], [2022, 268500], [2023, 286300], [2024, 301600], [2025, 316900]],
      yRange: [0, 350000],
      yTickFormat: function (v) { return v.toLocaleString('en-US', {minimumFractionDigits: 0, maximumFractionDigits: 0}); },
      valueFormat: function (v) { return v.toLocaleString('en-US', {minimumFractionDigits: 0, maximumFractionDigits: 0}); },
      yTickStep: 50000,
      annotations: [[2009, 196000, "196,000", "above"]],
      yearCheckpoints: [[72.125, 2009], [80.0, 2010.5], [102.55, 2025]],
      series2: { data: [[2009, 18.6], [2010, 18.1], [2011, 17.2], [2012, 16.5], [2013, 16.2], [2017, 18.0], [2018, 18.3], [2019, 18.3], [2020, 20.1], [2021, 20.5], [2022, 18.9], [2023, 18.8], [2024, 19.1], [2025, 19.4]], yRange: [0, 25], yTickFormat: function (v) { return v.toFixed(0) + "%"; }, valueFormat: function (v) { return v.toFixed(0) + "%"; }, yTickStep: 5, label: "share of all foreign workers" }
    },
    { src: KNIFE, type: "letterbox", zoom: [1, 1.07, 1.14], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-in-out" },
    { src: TAILOR, type: "letterbox", zoom: [1.12, 1.06, 1], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-out" },
    { src: SERVANTS, type: "letterbox", zoom: [1, 1.06, 1.12], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-in" },
    { src: KNIFE, type: "letterbox", zoom: [1.13, 1.06, 1], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-out" },
    { src: DHOBY, type: "letterbox", zoom: [1, 1.07, 1.14], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "ease-in-out" },
    { src: HERO, type: "letterbox", zoom: [1, 1.05, 1.1], pan: ["50% 50%", "50% 50%", "50% 50%"], ease: "linear" },
  ];

  var sentences = [{"text": "From Amah to Auntie: How Singapore Came to Rely on Foreign Domestic Workers", "offset_s": 0.0, "duration_s": 6.025}, {"text": "Before there were foreign domestic workers, there were amahs — Cantonese women, some of them majie who had taken vows never to marry, who ran Singaporean households through the mid-20th century.", "offset_s": 6.025, "duration_s": 12.875}, {"text": "By the 1970s they were a vanishing breed: as female literacy rose from 27% in 1957 to over 60% by 1970, the young women who might once have gone into domestic service were going into factories and offices instead.", "offset_s": 18.9, "duration_s": 18.575}, {"text": "Singapore's households still needed the help.", "offset_s": 37.475, "duration_s": 3.475}, {"text": "It just had to come from somewhere else.", "offset_s": 40.95, "duration_s": 3.0}, {"text": "That \"somewhere else\" became a formal system in 1978, when Singapore introduced a scheme allowing households to sponsor domestic workers from the region on work permits — one of the first countries in the world to do so.", "offset_s": 43.95, "duration_s": 15.875}, {"text": "Growth was slow at first: by some accounts there were only around 50,000 foreign domestic workers by 1990, enough for roughly one in every 13 households.", "offset_s": 59.825, "duration_s": 12.3}, {"text": "What followed was decades of steady expansion, tracked in the official numbers from 2009 onward.", "offset_s": 72.125, "duration_s": 7.625}, {"text": "What the chart doesn't show is quite as interesting as what it does.", "offset_s": 79.75, "duration_s": 4.8}, {"text": "Over this period, Singapore's total foreign workforce grew from about 1.05 million to 1.64 million — and foreign domestic workers have held fairly steady at somewhere between 16% and 20% of that total the whole way through.", "offset_s": 84.55, "duration_s": 18.0}, {"text": "In other words, the number of domestic workers hasn't been growing faster than Singapore's reliance on foreign labour generally; it's been growing in step with it, year after year, through a financial crisis, a pandemic, and every cooling and loosening of immigration policy in between.", "offset_s": 102.55, "duration_s": 19.075}, {"text": "Even the COVID-19 border closures barely dented the count — 247,400 in 2020 to 246,300 in 2021 — because most of the workers already here simply stayed, even as new arrivals slowed to a trickle.", "offset_s": 121.625, "duration_s": 19.275}, {"text": "By 2010, roughly one in every five Singaporean households employed a foreign domestic worker — up from about one in 13 just two decades earlier.", "offset_s": 140.9, "duration_s": 11.225}, {"text": "That ratio has essentially held since, even as the raw numbers kept climbing, which points to why: it's not that more Singaporeans are choosing to hire help so much as more households now need it, particularly as more women work full-time and the population ages.", "offset_s": 152.125, "duration_s": 17.35}, {"text": "The subsidised eldercare infrastructure covered elsewhere on this blog exists alongside, not instead of, hundreds of thousands of resident domestic workers doing that care privately, one household at a time.", "offset_s": 169.475, "duration_s": 14.95}, {"text": "Why it matters today: the amah generation is gone, but the gap they filled never closed — it was filled instead by an entire, largely invisible workforce whose numbers now rival a small city's, doing work that keeps a great many Singaporean households running without ever quite being counted as part of the story.", "offset_s": 184.425, "duration_s": 20.85}];

  var imageSchedule = [
    { t: 0.0, slide: 0 }, { t: 18.9, slide: 1 }, { t: 37.475, slide: 2 }, { t: 43.95, slide: 3 }, { t: 59.825, slide: 4 }, { t: 72.125, slide: 5 }, { t: 102.55, slide: 6 }, { t: 121.625, slide: 7 }, { t: 140.9, slide: 8 }, { t: 152.125, slide: 9 }, { t: 169.475, slide: 10 }, { t: 184.425, slide: 11 }
  ];
  var TOTAL_DURATION = 205.275;

  var MAX_CHARS = 100;
  function splitLongSentence(text, offset, duration) {
    if (text.length <= MAX_CHARS) return [{ text: text, offset_s: offset, duration_s: duration }];
    var parts = text.split(/(?<=[:;])\s+/);
    var chunks = [];
    parts.forEach(function (p) {
      if (p.length <= MAX_CHARS) { chunks.push(p); return; }
      var words = p.split(' ');
      var cur = '';
      words.forEach(function (w) {
        if ((cur + ' ' + w).trim().length > MAX_CHARS) { chunks.push(cur.trim()); cur = w; }
        else { cur = (cur + ' ' + w).trim(); }
      });
      if (cur) chunks.push(cur.trim());
    });
    var totalChars = chunks.reduce(function (a, c) { return a + c.length; }, 0);
    var t = offset;
    return chunks.map(function (c) {
      var share = duration * (c.length / totalChars);
      var entry = { text: c, offset_s: t, duration_s: share };
      t += share;
      return entry;
    });
  }
  var captionChunks = [];
  sentences.forEach(function (s) {
    captionChunks = captionChunks.concat(splitLongSentence(s.text, s.offset_s, s.duration_s));
  });

  var slideDurations = imageSchedule.map(function (entry, i) {
    var next = imageSchedule[i + 1];
    return (next ? next.t : TOTAL_DURATION) - entry.t;
  });

  var CHART_W = 1280, CHART_H = 720;
  var SVGNS = 'http://www.w3.org/2000/svg';
  function svgEl(tag, attrs) {
    var e = document.createElementNS(SVGNS, tag);
    for (var k in attrs) { e.setAttribute(k, attrs[k]); }
    return e;
  }

  function buildChartSlide(s) {
    var svg = svgEl('svg', { viewBox: '0 0 ' + CHART_W + ' ' + CHART_H, width: '100%', height: '100%', preserveAspectRatio: 'xMidYMid meet' });
    svg.style.cssText = 'position:absolute;inset:0;background:#1a1a19;';

    var left = CHART_W * 0.09, right = CHART_W * 0.95;
    var xMin = s.xRange[0], xMax = s.xRange[1];
    var FONT = 'system-ui,-apple-system,"Segoe UI",sans-serif';
    function sx(yr) { return left + (yr - xMin) / (xMax - xMin) * (right - left); }

    if (s.title) {
      var titleEl = svgEl('text', { x: left, y: CHART_H * 0.08, fill: '#fff', 'font-size': CHART_H * 0.032, 'font-weight': '600', 'font-family': FONT });
      titleEl.textContent = s.title;
      svg.appendChild(titleEl);
    }

    function piecewiseInterp(checkpoints, x) {
      if (x <= checkpoints[0][0]) return checkpoints[0][1];
      if (x >= checkpoints[checkpoints.length - 1][0]) return checkpoints[checkpoints.length - 1][1];
      for (var i = 0; i < checkpoints.length - 1; i++) {
        var x0 = checkpoints[i][0], y0 = checkpoints[i][1], x1 = checkpoints[i + 1][0], y1 = checkpoints[i + 1][1];
        if (x0 <= x && x <= x1) { var f = (x1 !== x0) ? (x - x0) / (x1 - x0) : 0; return y0 + (y1 - y0) * f; }
      }
      return checkpoints[checkpoints.length - 1][1];
    }
    function interpAt(data, year) {
      if (year <= data[0][0]) return data[0][1];
      if (year >= data[data.length - 1][0]) return data[data.length - 1][1];
      for (var i = 0; i < data.length - 1; i++) {
        var y0 = data[i][0], v0 = data[i][1], y1 = data[i + 1][0], v1 = data[i + 1][1];
        if (y0 <= year && year <= y1) { var f = (y1 !== y0) ? (year - y0) / (y1 - y0) : 0; return v0 + (v1 - v0) * f; }
      }
      return data[data.length - 1][1];
    }

    function makePanel(pdata, spec, pTop, pBot, drawXTicks, panelLabel) {
      var yMin = spec.yRange[0], yMax = spec.yRange[1];
      function sy(v) { return pTop + (yMax - v) / (yMax - yMin) * (pBot - pTop); }
      var yTickFormat = spec.yTickFormat || function (v) { return '$' + v; };
      var valueFormat = spec.valueFormat || function (v) { return '$' + Math.round(v) + ' / sqft'; };
      var step = spec.yTickStep || ((yMax <= 800) ? 100 : 200);
      for (var v = 0; v <= yMax; v += step) {
        var y = sy(v);
        svg.appendChild(svgEl('line', { x1: left, x2: right, y1: y, y2: y, stroke: v === 0 ? '#383835' : '#2c2c2a', 'stroke-width': 1 }));
        var lt = svgEl('text', { x: left - 10, y: y + 4, fill: '#898781', 'font-size': CHART_H * 0.022, 'text-anchor': 'end', 'font-family': FONT });
        lt.textContent = yTickFormat(v);
        svg.appendChild(lt);
      }
      if (drawXTicks) {
        for (var yr = xMin; yr <= xMax; yr += 5) {
          var xt = svgEl('text', { x: sx(yr), y: pBot + CHART_H * 0.045, fill: '#898781', 'font-size': CHART_H * 0.022, 'text-anchor': 'middle', 'font-family': FONT });
          xt.textContent = String(yr);
          svg.appendChild(xt);
        }
      }
      if (panelLabel) {
        var pl = svgEl('text', { x: left, y: pTop - CHART_H * 0.025, fill: '#c3c2b7', 'font-size': CHART_H * 0.02, 'font-family': FONT });
        pl.textContent = panelLabel;
        svg.appendChild(pl);
      }

      var d = pdata.map(function (p, i) { return (i === 0 ? 'M' : 'L') + sx(p[0]) + ',' + sy(p[1]); }).join(' ');
      var path = svgEl('path', {
        d: d, fill: 'none', stroke: '#3987e5', 'stroke-width': Math.max(2, CHART_H * 0.0035),
        'stroke-linejoin': 'round', 'stroke-linecap': 'round', 'pathLength': '1',
        'stroke-dasharray': '1', 'stroke-dashoffset': '1'
      });
      svg.appendChild(path);

      var annEls = (spec.annotations || []).map(function (a) {
        var ax = sx(a[0]), ay = sy(a[1]);
        var r = Math.max(3, CHART_H * 0.006);
        var g = svgEl('g', { opacity: 0 });
        g.appendChild(svgEl('circle', { cx: ax, cy: ay, r: r, fill: '#3987e5', stroke: '#1a1a19', 'stroke-width': 2 }));
        var ly = a[3] === 'below' ? ay + r + 14 : ay - r - 6;
        var lbl = svgEl('text', { x: ax, y: ly, fill: '#c3c2b7', 'font-size': CHART_H * 0.02, 'text-anchor': 'middle', 'font-family': FONT });
        lbl.textContent = a[2];
        g.appendChild(lbl);
        svg.appendChild(g);
        return { year: a[0], el: g };
      });

      var dot = svgEl('circle', { r: Math.max(4, CHART_H * 0.007), fill: '#3987e5', stroke: '#1a1a19', 'stroke-width': 2, opacity: 0 });
      svg.appendChild(dot);
      var valText = svgEl('text', { fill: '#fff', 'font-size': CHART_H * 0.026, 'font-weight': '600', 'font-family': FONT, opacity: 0 });
      svg.appendChild(valText);
      var yearText = svgEl('text', { fill: '#c3c2b7', 'font-size': CHART_H * 0.02, 'font-family': FONT, opacity: 0 });
      svg.appendChild(yearText);

      return function updatePanel(curYear, progress) {
        path.setAttribute('stroke-dashoffset', String(1 - progress));
        var curVal = interpAt(pdata, curYear);
        var ex = sx(curYear), ey = sy(curVal);
        dot.setAttribute('cx', ex); dot.setAttribute('cy', ey); dot.setAttribute('opacity', 1);
        var labelX = Math.min(ex + 14, right - CHART_W * 0.16);
        valText.setAttribute('x', labelX); valText.setAttribute('y', ey - CHART_H * 0.03);
        valText.textContent = valueFormat(curVal); valText.setAttribute('opacity', 1);
        yearText.setAttribute('x', labelX); yearText.setAttribute('y', ey - CHART_H * 0.005);
        yearText.textContent = String(Math.round(curYear)); yearText.setAttribute('opacity', 1);
        annEls.forEach(function (a) { a.el.setAttribute('opacity', curYear >= a.year - 0.01 ? 1 : 0); });
      };
    }

    var updaters;
    if (s.series2) {
      updaters = [
        makePanel(s.data, s, CHART_H * 0.15, CHART_H * 0.40, false, null),
        makePanel(s.series2.data, s.series2, CHART_H * 0.45, CHART_H * 0.60, true, s.series2.label)
      ];
    } else {
      updaters = [makePanel(s.data, s, CHART_H * 0.16, CHART_H * 0.82, true, null)];
    }

    function update(t, progress) {
      var curYear = s.yearCheckpoints ? piecewiseInterp(s.yearCheckpoints, t) : xMin + (xMax - xMin) * progress;
      updaters.forEach(function (u) { u(curYear, progress); });
    }

    return { svg: svg, update: update };
  }

  var viewer = document.getElementById('watch-viewer');
  var stage = document.getElementById('watch-stage');
  var captionEl = document.getElementById('watch-caption');
  var closeBtn = document.getElementById('watch-close');
  var playBtn = document.getElementById('watch-play');
  var progress = document.getElementById('watch-progress');
  var progressFill = document.getElementById('watch-progress-fill');
  var timeEl = document.getElementById('watch-time');
  var watchWidget = document.getElementById('watch-widget');

  // Separate Audio object from the Listen widget's <audio> element -
  // never share the `audio` variable across the two IIFEs (the #1
  // recurring bug in this pipeline's history, see
  // docs/production-pipeline.md section 13).
  var listenAudio = document.getElementById('listen-audio');
  var watchAudio = new Audio(listenAudio.querySelector('source').src);
  watchAudio.preload = 'none';

  var styleEl = document.createElement('style');
  document.head.appendChild(styleEl);

  var slideEls = slides.map(function (s, i) {
    var el = document.createElement('div');
    el.style.cssText = 'position:absolute;inset:0;opacity:0;transition:opacity 0.8s ease;';
    var ease = s.ease || 'linear';
    var dur = slideDurations[imageSchedule.findIndex(function (e) { return e.slide === i; })];

    if (s.type === 'chart') {
      var chart = buildChartSlide(s);
      el.appendChild(chart.svg);
      el._chartUpdate = chart.update;
    } else if (s.type === 'letterbox') {
      var bg = document.createElement('div');
      bg.style.cssText = 'position:absolute;inset:-8%;background-size:cover;background-position:center;filter:blur(30px) brightness(0.55);background-image:url(\'' + s.src + '\');';
      bg.style.animation = 'kb' + i + ' ' + dur + 's ' + ease + ' forwards';
      var fg = document.createElement('div');
      fg.style.cssText = 'position:absolute;inset:6%;background-size:contain;background-position:center;background-repeat:no-repeat;background-image:url(\'' + s.src + '\');';
      el.appendChild(bg); el.appendChild(fg);
      el._animTargets = [bg];
    } else {
      var layer = document.createElement('div');
      layer.style.cssText = 'position:absolute;inset:-8%;background-size:cover;background-image:url(\'' + s.src + '\');';
      layer.style.animation = 'kb' + i + ' ' + dur + 's ' + ease + ' forwards';
      el.appendChild(layer);
      el._animTargets = [layer];
    }
    if (s.type !== 'chart') {
      styleEl.textContent += '@keyframes kb' + i + ' { 0% { transform: scale(' + s.zoom[0] + '); background-position: ' + s.pan[0] + '; } 50% { transform: scale(' + s.zoom[1] + '); background-position: ' + s.pan[1] + '; } 100% { transform: scale(' + s.zoom[2] + '); background-position: ' + s.pan[2] + '; } }\n';
    }
    stage.appendChild(el);
    return el;
  });
  slides.forEach(function (s) { if (s.src) { var img = new Image(); img.src = s.src; } });

  var currentIndex = -1, currentSentenceIndex = -1;
  function fmtTime(t) {
    if (!isFinite(t)) return '0:00';
    var m = Math.floor(t / 60), s = Math.floor(t % 60);
    return m + ':' + (s < 10 ? '0' : '') + s;
  }
  function slideIndexForTime(t) {
    var idx = 0;
    for (var i = 0; i < imageSchedule.length; i++) { if (imageSchedule[i].t <= t) idx = imageSchedule[i].slide; else break; }
    return idx;
  }
  function sentenceIndexForTime(t) {
    var idx = 0;
    for (var i = 0; i < captionChunks.length; i++) { if (captionChunks[i].offset_s <= t) idx = i; else break; }
    return idx;
  }
  function updateForTime(t) {
    var sIdx = slideIndexForTime(t);
    if (sIdx !== currentIndex) {
      if (currentIndex >= 0) slideEls[currentIndex].style.opacity = '0';
      slideEls[sIdx].style.opacity = '1';
      currentIndex = sIdx;
    }
    if (slideEls[sIdx]._chartUpdate) {
      var schedIdx = imageSchedule.findIndex(function (e) { return e.slide === sIdx; });
      var segStart = imageSchedule[schedIdx].t;
      var nextEntry = imageSchedule[schedIdx + 1];
      var segEnd = nextEntry ? nextEntry.t : TOTAL_DURATION;
      var chartProgress = Math.min(1, Math.max(0, (t - segStart) / Math.max(0.001, segEnd - segStart)));
      slideEls[sIdx]._chartUpdate(t, chartProgress);
    }
    var cIdx = sentenceIndexForTime(t);
    if (cIdx !== currentSentenceIndex) { captionEl.textContent = captionChunks[cIdx].text; currentSentenceIndex = cIdx; }
    var pct = Math.min(100, (t / TOTAL_DURATION) * 100);
    progressFill.style.width = pct + '%';
    timeEl.textContent = fmtTime(t) + ' / ' + fmtTime(TOTAL_DURATION);
  }
  function openViewer() {
    viewer.style.display = 'block';
    currentIndex = -1; currentSentenceIndex = -1;
    slideEls.forEach(function (el) { el.style.opacity = '0'; });
    watchAudio.currentTime = 0;
    updateForTime(0);
    watchAudio.play();
  }
  function closeViewer() { viewer.style.display = 'none'; watchAudio.pause(); }

  watchWidget.addEventListener('click', openViewer);
  watchWidget.addEventListener('keydown', function (e) { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); openViewer(); } });
  closeBtn.addEventListener('click', closeViewer);
  playBtn.addEventListener('click', function () { if (watchAudio.paused) watchAudio.play(); else watchAudio.pause(); });
  watchAudio.addEventListener('play', function () { playBtn.innerHTML = '&#10074;&#10074;'; });
  watchAudio.addEventListener('pause', function () { playBtn.innerHTML = '&#9654;'; });
  watchAudio.addEventListener('timeupdate', function () { updateForTime(watchAudio.currentTime); });
  watchAudio.addEventListener('ended', closeViewer);
  progress.addEventListener('click', function (e) {
    var rect = progress.getBoundingClientRect();
    var pct = (e.clientX - rect.left) / rect.width;
    watchAudio.currentTime = pct * TOTAL_DURATION;
  });
})();
</script>

<!-- WATCH-WIDGET:END -->

![Lucky Plaza, Orchard Road, Singapore](https://upload.wikimedia.org/wikipedia/commons/0/04/Lucky_Plaza%2C_Orchard_Road%2C_Singapore.jpg)

*Lucky Plaza on Orchard Road, photographed on a Sunday — since the late 1980s, a de facto gathering place for the city's Filipino domestic workers on their day off. (Photo: Exec8 / Wikimedia Commons, CC BY-SA 3.0)*

That "somewhere else" became a formal system in 1978, when Singapore introduced a scheme allowing households to sponsor domestic workers from the region on work permits — one of the first countries in the world to do so. Growth was slow at first: by some accounts there were only around 50,000 foreign domestic workers by 1990, enough for roughly one in every 13 households. What followed was decades of steady expansion, tracked in the official numbers from 2009 onward.

<div class="fdw-chart">
<style>
.fdw-chart {
  --surface-1: #1a1a19;
  --text-primary: #ffffff;
  --text-secondary: #c3c2b7;
  --text-muted: #898781;
  --grid-line: #2c2c2a;
  --baseline: #383835;
  --series-1: #3987e5;
  --tooltip-bg: #0d0d0d;
  color-scheme: dark;
  background: var(--surface-1);
  border-radius: 8px;
  padding: 1.25em 1.25em 0.75em;
  margin: 1.5em 0;
  font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
}
.fdw-chart .fdw-title {
  color: var(--text-primary);
  font-size: 0.95em;
  font-weight: 600;
  margin: 0 0 0.9em;
}
.fdw-chart svg { display: block; width: 100%; height: auto; }
.fdw-chart .fdw-grid { stroke: var(--grid-line); stroke-width: 1; }
.fdw-chart .fdw-baseline { stroke: var(--baseline); stroke-width: 1; }
.fdw-chart .fdw-tick { fill: var(--text-muted); font-size: 11px; }
.fdw-chart .fdw-line { fill: none; stroke: var(--series-1); stroke-width: 2; stroke-linejoin: round; stroke-linecap: round; }
.fdw-chart .fdw-line-gap { fill: none; stroke: var(--series-1); stroke-width: 2; stroke-linejoin: round; stroke-linecap: round; stroke-dasharray: 4 4; opacity: 0.6; }
.fdw-chart .fdw-endlabel { fill: var(--text-primary); font-size: 12px; font-weight: 600; }
.fdw-chart .fdw-endlabel-sub { fill: var(--text-secondary); font-size: 10px; }
.fdw-chart .fdw-peaklabel { fill: var(--text-secondary); font-size: 10px; }
.fdw-chart .fdw-panel-label { fill: var(--text-secondary); font-size: 10px; }
.fdw-chart .fdw-dot { fill: var(--series-1); stroke: var(--surface-1); stroke-width: 2; }
.fdw-chart .fdw-crosshair { stroke: var(--text-muted); stroke-width: 1; stroke-dasharray: 2 2; opacity: 0; }
.fdw-chart .fdw-hoverdot { fill: var(--series-1); stroke: var(--surface-1); stroke-width: 2; opacity: 0; }
.fdw-chart .fdw-tooltip-bg { fill: var(--tooltip-bg); opacity: 0; }
.fdw-chart .fdw-tooltip-year { fill: var(--text-secondary); font-size: 10px; opacity: 0; }
.fdw-chart .fdw-tooltip-val { fill: var(--text-primary); font-size: 12px; font-weight: 600; opacity: 0; }
.fdw-chart .fdw-hitrect { fill: transparent; cursor: crosshair; }
.fdw-chart details { margin-top: 0.75em; }
.fdw-chart summary { color: var(--text-secondary); font-size: 0.85em; cursor: pointer; }
.fdw-chart table { width: 100%; border-collapse: collapse; margin-top: 0.6em; font-size: 0.85em; color: var(--text-secondary); }
.fdw-chart th, .fdw-chart td { text-align: right; padding: 2px 8px; border-bottom: 1px solid var(--grid-line); font-variant-numeric: tabular-nums; }
.fdw-chart th:first-child, .fdw-chart td:first-child { text-align: left; }
</style>

<p class="fdw-title">Foreign domestic workers in Singapore, Work Permit holders, year-end (2009&ndash;2025)</p>

<svg viewBox="0 0 720 430" role="img" aria-label="Two stacked line charts sharing a 2009 to 2025 time axis. Top: the number of foreign domestic workers in Singapore rises from 196,000 in 2009 to 316,900 in 2025 (2014 to 2016 not published, shown as a dashed gap). Bottom: foreign domestic workers as a share of Singapore's total foreign workforce stays within a 16 to 20 percent band over the same period, dipping to about 16 percent in 2013 and peaking near 20.5 percent in 2021.">
  <g id="fdw-gridlines"></g>
  <g id="fdw-xticks"></g>
  <text class="fdw-panel-label" x="46" y="266">Share of all foreign workers</text>
  <path id="fdw-line1" class="fdw-line"></path>
  <path id="fdw-linegap" class="fdw-line-gap"></path>
  <path id="fdw-line2" class="fdw-line"></path>
  <path id="fdw-p2-line1" class="fdw-line"></path>
  <path id="fdw-p2-linegap" class="fdw-line-gap"></path>
  <path id="fdw-p2-line2" class="fdw-line"></path>
  <g id="fdw-annotations"></g>
  <line id="fdw-crosshair" class="fdw-crosshair" y1="24" y2="392"></line>
  <circle id="fdw-hoverdot" class="fdw-hoverdot" r="4"></circle>
  <circle id="fdw-hoverdot2" class="fdw-hoverdot" r="4"></circle>
  <g id="fdw-tooltip">
    <rect id="fdw-tooltip-bg" class="fdw-tooltip-bg" rx="4" width="120" height="48"></rect>
    <text id="fdw-tooltip-year" class="fdw-tooltip-year"></text>
    <text id="fdw-tooltip-val" class="fdw-tooltip-val"></text>
    <text id="fdw-tooltip-pct" class="fdw-tooltip-year"></text>
  </g>
  <rect id="fdw-hitrect" class="fdw-hitrect" x="46" y="24" width="610" height="368"></rect>
</svg>

<details>
<summary>View data as a table</summary>
<table>
<thead><tr><th>Year</th><th>Foreign domestic workers</th><th>Total foreign workforce</th><th>FDW share</th></tr></thead>
<tbody>
<tr><td>2009</td><td>196,000</td><td>1,053,500</td><td>18.6%</td></tr>
<tr><td>2010</td><td>201,400</td><td>1,113,200</td><td>18.1%</td></tr>
<tr><td>2011</td><td>206,300</td><td>1,197,900</td><td>17.2%</td></tr>
<tr><td>2012</td><td>209,600</td><td>1,268,300</td><td>16.5%</td></tr>
<tr><td>2013</td><td>214,500</td><td>1,321,600</td><td>16.2%</td></tr>
<tr><td>2014&ndash;2016</td><td>not published</td><td>not published</td><td>&mdash;</td></tr>
<tr><td>2017</td><td>246,800</td><td>1,368,000</td><td>18.0%</td></tr>
<tr><td>2018</td><td>253,800</td><td>1,386,000</td><td>18.3%</td></tr>
<tr><td>2019</td><td>261,800</td><td>1,427,500</td><td>18.3%</td></tr>
<tr><td>2020</td><td>247,400</td><td>1,231,500</td><td>20.1%</td></tr>
<tr><td>2021</td><td>246,300</td><td>1,200,400</td><td>20.5%</td></tr>
<tr><td>2022</td><td>268,500</td><td>1,424,200</td><td>18.9%</td></tr>
<tr><td>2023</td><td>286,300</td><td>1,525,500</td><td>18.8%</td></tr>
<tr><td>2024</td><td>301,600</td><td>1,576,500</td><td>19.1%</td></tr>
<tr><td>2025</td><td>316,900</td><td>1,635,700</td><td>19.4%</td></tr>
</tbody>
</table>
</details>

<p style="color: var(--text-muted); font-size: 0.8em; margin-top: 0.6em;">Source: Ministry of Manpower &mdash; foreign domestic worker counts and total foreign workforce, year-end. Recent years via data.gov.sg and mom.gov.sg; 2009&ndash;2013 totals from MOM's historical releases. 2014&ndash;2016 figures were not published (dashed segment). FDW share is the FDW count divided by the total foreign workforce.</p>
</div>

<script>
(function () {
  var countData = [[2009,196000],[2010,201400],[2011,206300],[2012,209600],[2013,214500],[2017,246800],[2018,253800],[2019,261800],[2020,247400],[2021,246300],[2022,268500],[2023,286300],[2024,301600],[2025,316900]];
  var pctData = [[2009,18.6],[2010,18.1],[2011,17.2],[2012,16.5],[2013,16.2],[2017,18.0],[2018,18.3],[2019,18.3],[2020,20.1],[2021,20.5],[2022,18.9],[2023,18.8],[2024,19.1],[2025,19.4]];

  var scope = document.currentScript.parentElement.querySelector('.fdw-chart');
  if (!scope) { return; }
  var svgEl = scope.querySelector('svg');

  var left = 46, right = 656, xMin = 2009, xMax = 2025;
  function sx(year) { return left + (year - xMin) / (xMax - xMin) * (right - left); }
  function fmt(n) { return n.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ','); }

  var NS = 'http://www.w3.org/2000/svg';
  function el(tag, attrs) {
    var e = document.createElementNS(NS, tag);
    for (var k in attrs) { e.setAttribute(k, attrs[k]); }
    return e;
  }

  var gridG = scope.querySelector('#fdw-gridlines');
  var annoG = scope.querySelector('#fdw-annotations');

  // Draws one panel: gridlines + y-ticks, its line (with the 2014-16
  // dashed gap, since both series share those missing years), and start/
  // end annotation dots. Returns { sy } for the hover handler.
  function drawPanel(cfg) {
    var data = cfg.data, yMax = cfg.yMax, pTop = cfg.top, pBot = cfg.bottom;
    function sy(val) { return pTop + (yMax - val) / (yMax - cfg.yMin) * (pBot - pTop); }

    for (var v = cfg.yMin; v <= yMax + 0.0001; v += cfg.tickStep) {
      var y = sy(v);
      gridG.appendChild(el('line', { class: v === 0 ? 'fdw-baseline' : 'fdw-grid', x1: left, x2: right, y1: y, y2: y }));
      var lt = el('text', { class: 'fdw-tick', x: left - 8, y: y + 3, 'text-anchor': 'end' });
      lt.textContent = cfg.tickFmt(v);
      gridG.appendChild(lt);
    }

    var gapIndex = -1;
    for (var i = 1; i < data.length; i++) {
      if (data[i][0] - data[i - 1][0] > 1) { gapIndex = i; break; }
    }
    var seg1 = gapIndex === -1 ? data : data.slice(0, gapIndex);
    var seg2 = gapIndex === -1 ? [] : data.slice(gapIndex);
    scope.querySelector(cfg.line1Id).setAttribute('d', seg1.map(function (p, i) {
      return (i === 0 ? 'M' : 'L') + sx(p[0]) + ',' + sy(p[1]);
    }).join(' '));
    if (gapIndex !== -1) {
      var gs = data[gapIndex - 1], ge = data[gapIndex];
      scope.querySelector(cfg.gapId).setAttribute('d', 'M' + sx(gs[0]) + ',' + sy(gs[1]) + ' L' + sx(ge[0]) + ',' + sy(ge[1]));
      scope.querySelector(cfg.line2Id).setAttribute('d', seg2.map(function (p, i) {
        return (i === 0 ? 'M' : 'L') + sx(p[0]) + ',' + sy(p[1]);
      }).join(' '));
    }

    if (cfg.startLabel) {
      var s0 = data[0], sX = sx(s0[0]), sY = sy(s0[1]);
      annoG.appendChild(el('circle', { class: 'fdw-dot', cx: sX, cy: sY, r: 3 }));
      var sl = el('text', { class: 'fdw-peaklabel', x: sX + 6, y: sY - 8, 'text-anchor': 'start' });
      sl.textContent = cfg.startLabel;
      annoG.appendChild(sl);
    }
    var e0 = data[data.length - 1], eX = sx(e0[0]), eY = sy(e0[1]);
    annoG.appendChild(el('circle', { class: 'fdw-dot', cx: eX, cy: eY, r: 4 }));
    var elb = el('text', { class: 'fdw-endlabel', x: eX - 4, y: eY - 12, 'text-anchor': 'end' });
    elb.textContent = cfg.endLabel;
    annoG.appendChild(elb);
    var es = el('text', { class: 'fdw-endlabel-sub', x: eX - 4, y: eY - 24, 'text-anchor': 'end' });
    es.textContent = '2025';
    annoG.appendChild(es);

    return { sy: sy };
  }

  var p1 = drawPanel({
    data: countData, yMin: 0, yMax: 350000, top: 24, bottom: 240,
    tickStep: 50000, tickFmt: function (v) { return (v / 1000) + 'K'; },
    line1Id: '#fdw-line1', gapId: '#fdw-linegap', line2Id: '#fdw-line2',
    startLabel: '196,000 (2009)', endLabel: '316,900'
  });
  var p2 = drawPanel({
    data: pctData, yMin: 0, yMax: 25, top: 278, bottom: 392,
    tickStep: 5, tickFmt: function (v) { return v + '%'; },
    line1Id: '#fdw-p2-line1', gapId: '#fdw-p2-linegap', line2Id: '#fdw-p2-line2',
    startLabel: null, endLabel: '19.4%'
  });

  var xG = scope.querySelector('#fdw-xticks');
  for (var yr = 2009; yr <= 2025; yr += 4) {
    var xt = el('text', { class: 'fdw-tick', x: sx(yr), y: 410, 'text-anchor': 'middle' });
    xt.textContent = String(yr);
    xG.appendChild(xt);
  }

  var hit = scope.querySelector('#fdw-hitrect');
  var crosshair = scope.querySelector('#fdw-crosshair');
  var hoverDot = scope.querySelector('#fdw-hoverdot');
  var hoverDot2 = scope.querySelector('#fdw-hoverdot2');
  var tipBg = scope.querySelector('#fdw-tooltip-bg');
  var tipYear = scope.querySelector('#fdw-tooltip-year');
  var tipVal = scope.querySelector('#fdw-tooltip-val');
  var tipPct = scope.querySelector('#fdw-tooltip-pct');

  function nearestIndex(year) {
    var best = 0, bestDist = Infinity;
    for (var i = 0; i < countData.length; i++) {
      var d = Math.abs(countData[i][0] - year);
      if (d < bestDist) { bestDist = d; best = i; }
    }
    return best;
  }

  function handleMove(clientX) {
    var rect = svgEl.getBoundingClientRect();
    var scale = 720 / rect.width;
    var svgX = (clientX - rect.left) * scale;
    var year = xMin + (svgX - left) / (right - left) * (xMax - xMin);
    year = Math.max(xMin, Math.min(xMax, year));
    var idx = nearestIndex(year);
    var cPt = countData[idx], pPt = pctData[idx];
    var px = sx(cPt[0]);
    var cY = p1.sy(cPt[1]), pY = p2.sy(pPt[1]);

    crosshair.setAttribute('x1', px);
    crosshair.setAttribute('x2', px);
    crosshair.style.opacity = 1;
    hoverDot.setAttribute('cx', px); hoverDot.setAttribute('cy', cY); hoverDot.style.opacity = 1;
    hoverDot2.setAttribute('cx', px); hoverDot2.setAttribute('cy', pY); hoverDot2.style.opacity = 1;

    var flip = px > (left + right) / 2;
    var boxW = 120, boxH = 48;
    var boxX = flip ? px - boxW - 10 : px + 10;
    var boxY = Math.max(24, cY - boxH - 8);
    tipBg.setAttribute('x', boxX);
    tipBg.setAttribute('y', boxY);
    tipBg.setAttribute('width', boxW);
    tipBg.setAttribute('height', boxH);
    tipBg.style.opacity = 0.95;
    tipYear.setAttribute('x', boxX + 10);
    tipYear.setAttribute('y', boxY + 14);
    tipYear.textContent = String(cPt[0]);
    tipYear.style.opacity = 1;
    tipVal.setAttribute('x', boxX + 10);
    tipVal.setAttribute('y', boxY + 29);
    tipVal.textContent = fmt(cPt[1]) + ' FDWs';
    tipVal.style.opacity = 1;
    tipPct.setAttribute('x', boxX + 10);
    tipPct.setAttribute('y', boxY + 42);
    tipPct.textContent = 'share: ' + pPt[1] + '%';
    tipPct.style.opacity = 1;
  }

  function handleLeave() {
    crosshair.style.opacity = 0;
    hoverDot.style.opacity = 0;
    hoverDot2.style.opacity = 0;
    tipBg.style.opacity = 0;
    tipYear.style.opacity = 0;
    tipVal.style.opacity = 0;
    tipPct.style.opacity = 0;
  }

  hit.addEventListener('pointermove', function (e) { handleMove(e.clientX); });
  hit.addEventListener('pointerleave', handleLeave);
  hit.addEventListener('touchmove', function (e) { if (e.touches[0]) { handleMove(e.touches[0].clientX); } }, { passive: true });
})();
</script>

What the chart doesn't show is quite as interesting as what it does. Over this period, Singapore's total foreign workforce grew from about 1.05 million to 1.64 million — and foreign domestic workers have held fairly steady at somewhere between 16% and 20% of that total the whole way through. In other words, the number of domestic workers hasn't been growing faster than Singapore's reliance on foreign labour generally; it's been growing in step with it, year after year, through a financial crisis, a pandemic, and every cooling and loosening of immigration policy in between. Even the COVID-19 border closures barely dented the count — 247,400 in 2020 to 246,300 in 2021 — because most of the workers already here simply stayed, even as new arrivals slowed to a trickle.

By 2010, roughly one in every five Singaporean households employed a foreign domestic worker — up from about one in 13 just two decades earlier. That ratio has essentially held since, even as the raw numbers kept climbing, which points to why: it's not that more Singaporeans are choosing to hire help so much as more households now need it, particularly as more women work full-time and the population ages. The [subsidised eldercare infrastructure covered elsewhere on this blog]({{ '/2026/07/18/a-typical-day-for-singapores-silver-generation/' | relative_url }}) exists alongside, not instead of, hundreds of thousands of resident domestic workers doing that care privately, one household at a time. [See four more historical photos related to this post →](/gallery/from-amah-to-auntie-rise-of-domestic-workers/)

**Why it matters today:** the amah generation is gone, but the gap they filled never closed — it was filled instead by an entire, largely invisible workforce whose numbers now rival a small city's, doing work that keeps a great many Singaporean households running without ever quite being counted as part of the story.

---

**Sources:**
- [A Lifetime of Labour: Cantonese Amahs in Singapore — BiblioAsia, National Library Board](https://biblioasia.nlb.gov.sg/vol-13/issue-3/oct-dec-2017/lifetime-of-labour/)
- [Majie — National Library Board](https://www.nlb.gov.sg/main/article-detail?cmsuuid=a94ecf3b-09dc-4c55-9f8f-cff3db0ff92a)
- [Foreign Domestic Workers in Singapore — Lee Kuan Yew School of Public Policy, NUS](https://lkyspp.nus.edu.sg/docs/default-source/case-studies/fdws_in_singapore.pdf)
- [Maids in Singaporean households no longer a luxury — The Independent Singapore](https://theindependent.sg/maids-in-singaporean-households-no-longer-a-luxury-dependence-on-fdws-projected-to-rise/)
- [Fact sheet: Foreign domestic workers in Singapore (basic statistics) — TWC2](https://twc2.org.sg/2011/11/16/fact-sheet-foreign-domestic-workers-in-singapore-basic-statistics/)
- [Foreign workforce numbers — Ministry of Manpower](https://www.mom.gov.sg/foreign-workforce-numbers)
- [Number of Foreign Domestic Workers and Construction, Marine, Process Work Permit Holders — MOM, via data.gov.sg](https://data.gov.sg/datasets/d_4fb775d3cc311989261fae4ad22dde09/view)
- [Total Foreign Workforce — MOM, via data.gov.sg](https://data.gov.sg/datasets/d_5105c8c24199312306d1dd38bbc560a8/view)
- [File:Lucky Plaza, Orchard Road, Singapore.jpg — Wikimedia Commons](https://commons.wikimedia.org/wiki/File:Lucky_Plaza,_Orchard_Road,_Singapore.jpg)
- [File:KITLV - 29190 - Ten domestic servants of various ethnic origins, Singapore - circa 1900 — Wikimedia Commons](https://commons.wikimedia.org/wiki/File:KITLV_-_29190_-_Ten_domestic_servants_of_various_ethnic_origins,_each_with_an_object_relating_tot_their_task,_Singapore_-_circa_1900.tif)
- [File:KITLV - 103979 - Washing and bleaching field, Singapore - circa 1880 — Wikimedia Commons](https://commons.wikimedia.org/wiki/File:KITLV_-_103979_-_Washing_and_bleaching_field,_Singapore_-_circa_1880.tif)
- [File:KITLV - 29191 - Klingalese tailor Singapore - circa 1900 — Wikimedia Commons](https://commons.wikimedia.org/wiki/File:KITLV_-_29191_-_Klingalese_tailor_Singapore_-_circa_1900.tif)
- [File:KITLV - 50183 - Lambert & Co., G.R. - Singapore - Malaysian knife sharpeners in Singapore - circa 1900 — Wikimedia Commons](https://commons.wikimedia.org/wiki/File:KITLV_-_50183_-_Lambert_%26_Co.,_G.R._-_Singapore_-_Malaysian_knife_sharpeners_in_Singapore_-_circa_1900.tif)

[← Back to all posts](/)
